[Unit]
Description=Service FastAPI Application
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/opt/fastapi_app
ExecStart=/opt/fastapi_app/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
